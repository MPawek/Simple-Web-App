Work in progress - This is designed to be a web app that outputs a minified JSON object when the address is visited. Dockerfile has been included to automatically store image on DockerHub, and GitHub Actions have been developed to verify contents of code. 

Note that this setup has only been designed for a single person to use at a time, and may experience trouble with multiple users committing changes and pushing updates at the same time.
